//import { getDomainData } from '../../../src/shared/get-domain-data';

describe('When getDomainData is called with', () => {
    it('', () => {
        expect(true).toStrictEqual(true);
    });
    // it('a simple domain', () => {
    //     const urlSegment = 'DOMAIN.com';
    //     const { domain } = getDomainData(urlSegment);
    //     expect(getDomainData(urlSegment)).toStrictEqual(domain.toLowerCase());
    // });
    // it('a simple subdomain', () => {
    //     const urlSegment = 'SUBDOMAIN';
    //     const { subdomain } = getDomainData(urlSegment);
    //     expect(getDomainData(urlSegment)).toStrictEqual(subdomain.toLowerCase());
    // });
    // it('a domain with a short queryString', () => {
    //     const query = ['a'];
    //     const urlSegment = `domain.com?${query.join('&')}`;
    //     const { queryStrings } = getDomainData(urlSegment);
    //     expect(query).toStrictEqual(queryStrings);
    // });
    // it('a domain with a short pathname', () => {
    //     const path = 'a';
    //     const urlSegment = `domain.com/${path}`;
    //     const { pathname } = getDomainData(urlSegment);
    //     expect(path).toStrictEqual(pathname);
    // });
});
